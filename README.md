# WordPress Connector

Generic GitHub-controlled WordPress runtime bridge for safe discovery, read, patch, media, Elementor, Gutenberg, ACF and WooCommerce workflows.

Development happens through feature branches and reviewed pull requests. Production writes require an explicitly connected WordPress runtime and are staging-first until runtime acceptance has passed.
