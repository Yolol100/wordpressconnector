name: Audit Updater Apply

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: contains(github.event.head_commit.message, '[apply-updater-digests]')
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout exact audit revision
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 2
      - name: Apply updater hardening
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/apply-updater-hardening.py
          git diff --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add plugin/wordpressconnector/includes/Adapters
          git commit -m 'Bind self-update to GitHub asset digests and SBOM'
          git push origin HEAD:main
