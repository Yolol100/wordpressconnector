name: Temporary DoctorCura Pagination Fix

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/temp-doctorcura-pagination-fix.yml'
      - 'scripts/temp-doctorcura-pagination-fix.py'

permissions:
  contents: read

jobs:
  fix:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      SITE_URL: ${{ vars.DOCTORCURA_SITE_URL }}
      REST_USERNAME: ${{ secrets.DOCTORCURA_REST_USERNAME }}
      REST_APP_PASSWORD: ${{ secrets.DOCTORCURA_REST_APPLICATION_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - name: Apply guarded pagination fix
        run: python3 scripts/temp-doctorcura-pagination-fix.py
      - name: Upload verification
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: doctorcura-pagination-fix-verification
          path: doctorcura-pagination-fix-verification.json
          if-no-files-found: error
          retention-days: 1
